'use strict';

const firstNum = prompt("Please enter first number", );
const secondNum = prompt("Please enter first number", );
const thirdNum = prompt("Please enter first number", );

alert(`(${firstNum} + ${secondNum} + ${thirdNum}) / 3 = ${(+firstNum + +secondNum + +thirdNum)/3}`)

