'use strict';

function bringToPowerOf(number, power = 1){
    if(isNaN.number || isNaN.power) return console.log("Error");
    return number**power;
}
const number = +prompt("Please enter your number", '');
const power = +prompt("Please enter your number", '');

const result = bringToPowerOf(number, power);
alert(result);